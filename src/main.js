import { createApp } from 'vue'
import App from './App'

createApp(App).mount('#app')

// \
// 'number-button': !isNaN(btn.name) || ['.', '<='].includes(btn.name), 'operators': ['/', 'x', '+', '-', 'C', '+/-', '%'].includes(btn.name), 'whiteTheme': !darkTheme}